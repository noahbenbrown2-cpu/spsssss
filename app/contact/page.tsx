import { QuoteForm } from "@/components/QuoteForm";
import { site } from "@/lib/site";
export const metadata = { title: "Contact & Quote", description: "Request a quote from Set Piece Solutions." };
export default function Page(){return <section className="py-20"><div className="container grid gap-10 lg:grid-cols-2"><div><p className="font-black uppercase tracking-widest text-brand-red">Get a Quote</p><h1 className="mt-3 text-5xl font-black tracking-tight">Ready to make move-in ready easy?</h1><p className="mt-5 text-lg leading-8 text-neutral-700">Tell us about your apartment, rental, Airbnb, delivery, staging, move, or cleanout project. We will follow up quickly.</p><div className="mt-8 rounded-3xl bg-brand-cream p-6"><p className="font-black">Call</p><a href={`tel:${site.phoneHref}`} className="mt-2 block text-2xl font-black text-brand-red">{site.phoneDisplay}</a><p className="mt-5 font-black">Email</p><a href={`mailto:${site.email}`} className="mt-2 block font-bold text-brand-red">{site.email}</a></div></div><QuoteForm /></div></section>}
